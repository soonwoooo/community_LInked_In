import styled from "styled-components";
const ProfileTitle = () => {
  return <ProfileTitleWrapper>123</ProfileTitleWrapper>;
};

export default ProfileTitle;

const ProfileTitleWrapper = styled.div``;
